# Dependencies for the Python backup/sync utilities (aws_sync.py, backup_scheduler.py).
# NOT used by the Cloudflare Worker, which has no Python runtime.
#
# Pinned exactly rather than floored (>=). An open lower bound means a compromised
# or breaking upstream release is pulled automatically on the next install, with
# nothing in the repo recording which version was actually reviewed. Flagged as
# CWE-1104 / T1195.002 (unpinned dependency) by the red-team supply-chain check.
# Bump these deliberately, not implicitly.
boto3==1.42.19
requests==2.32.5
schedule==1.2.2
